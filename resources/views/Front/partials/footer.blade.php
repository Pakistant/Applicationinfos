<footer class="site-footer"><div class="footer-inner">
  <div><a href="{{ route('home') }}" class="brand footer-brand"><span class="brand-mark">Actu</span>Infos<span class="brand-dot">.</span></a><p style="margin-top:15px;max-width:320px">L’essentiel de l’actualité, des analyses utiles et des histoires qui méritent votre attention.</p></div>
  <div><h3>Navigation</h3><p><a href="{{ route('home') }}">Accueil</a></p><p><a href="{{ route('contact.front') }}">Nous contacter</a></p>@foreach($global_categories->take(3) as $category)<p><a href="{{ route('category.article', $category->slug) }}">{{ $category->name }}</a></p>@endforeach</div>
  <div><h3>Restons connectés</h3><p>{{ optional($global_setting)->email }}</p><div class="social-links">@foreach($global_social as $item)<a href="{{ $item->link }}" aria-label="{{ $item->name }}"><i class="{{ $item->icon }}"></i></a>@endforeach</div></div>
</div><div class="footer-bottom">© {{ now()->year }} {{ optional($global_setting)->web_site_name ?? 'ActuInfos' }} — Tous droits réservés.</div></footer>
