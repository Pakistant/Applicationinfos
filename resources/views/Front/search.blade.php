@extends('Front.app')
@section('title','Recherche — ActuInfos')
@section('Main_section')
  <p class="eyebrow">Résultats de recherche</p><h1 class="section-heading" style="font-size:2.4rem">Recherche : « {{ $searchKey }} »</h1>
  @if($articles->isNotEmpty())<div class="story-grid">@foreach($articles as $article)<article class="story-card"><a class="story-image" href="{{ route('article.details', $article->slug) }}"><img src="{{ $article->imageUrl() }}" alt="{{ $article->title }}"></a><div class="story-body"><a class="article-category" href="{{ route('category.article', $article->category->slug) }}">{{ $article->category->name }}</a><h2><a href="{{ route('article.details', $article->slug) }}">{{ $article->title }}</a></h2><p>{{ Str::limit(strip_tags(html_entity_decode($article->description)), 130) }}</p><div class="story-foot"><span>{{ $article->author->name }}</span><span>{{ $article->created_at->isoFormat('D MMM YYYY') }}</span></div></div></article>@endforeach</div><div class="mt-4">{{ $articles->links() }}</div>@else<div class="empty-state"><i class="fas fa-search" style="font-size:1.8rem;color:var(--brand)"></i><p style="margin:10px 0 0">Aucun résultat trouvé pour « {{ $searchKey }} ». Essayez un autre terme.</p></div>@endif
@endsection
